# Script to start "protocol" on the device, which has a very rudimentary
# shell.
#
base=/system
export CLASSPATH=$base/framework/protocol.jar
exec app_process $base/bin com.dji.commands.protocol.Protocol "$@"

